# Runtime dependencies. For the test suite, install the dev extra instead:
#   pip install -e ".[dev]"
# For the optional GPU backend (--gpu), install the gpu extra:
#   pip install -e ".[gpu]"
pygame>=2.5.0
numpy>=1.24.0
matplotlib>=3.7.0
